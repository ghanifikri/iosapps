//
//  ContentView.swift
//  iosapps
//
//  Created by mac on 22/05/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, worldd!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
